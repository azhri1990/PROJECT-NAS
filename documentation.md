# Documentation Standard

Documentation must answer: what, why, where, how, and what is authoritative.

## Canonical sources

Every durable concept should have one canonical source. Summaries, indexes, and AI-generated views must link back to it.

## Lifecycle metadata

Where lifecycle matters, use one of:
`draft`, `active`, `deprecated`, `archived`.
