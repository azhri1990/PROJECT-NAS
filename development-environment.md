# Development Environment Standard

A development environment must be reproducible from documented prerequisites and commands.

Required tools and versions should be explicit when they materially affect behavior. Setup instructions must distinguish required from optional tooling and must not depend on undocumented local state.
